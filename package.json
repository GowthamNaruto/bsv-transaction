{
  "name": "bsv-test-transaction",
  "version": "1.0.0",
  "description": "Creating a public/private key pair. Then, sending BSV to the public address from Money Button. Then, sending the BSV from that public address to another public address, or back to Money Button.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gowtham Selvam",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "bsv": "^1.0.0"
  }
}
